import apiConfig from "@/common/conf/index";
let { host = "" } = apiConfig || {};

export default {
  // commonUserInfoUrl: `${host}/common/userInfo`, //获取用户信息

  
  // fileUploadUrl: `${host}/file/upload`, //更新服务商详情
};
